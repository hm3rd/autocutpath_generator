# autocutpath_generator
アクリルスタンドを作成する際のベクターデータ(.svg)を自動で作成するアプリです。

## 使用方法
autocutpath_generator.zipを解凍し、実行する。windowsのセキュリティに引っ掛かるが実行できる。(もちろん悪意のあるファイルではないが、何かあった場合は自己責任でお願いします。)

## 実行
ファイルを実行(もしくはプログラムから実行)するとGUIが立ち上がる
作成したい画像(.png)を指定する。ここで使用する画像は作成したい部分が切り取られている(透過ファイル)前提です。

##  参考画像
!["代替テキスト"]("images/image.png")
